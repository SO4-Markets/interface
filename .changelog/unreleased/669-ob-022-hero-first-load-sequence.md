---
section: Features
description: Give the hero section a deliberate first-load sequence
---

The landing page hero now reveals elements in a staggered sequence: title animates in, followed by CTA and stats, then feature grid. This creates visual hierarchy and guides user attention through the landing experience on first load.

---
section: Features
description: Keep pending toasts persistent and terminal updates dismissible
---

Progress toasts for in-flight transactions now stay visible until the user dismisses them or a terminal outcome arrives. Terminal outcomes (success or error) are auto-dismissible but can be manually closed for faster clearing. This gives users full control over transaction visibility while maintaining clear completion signals.

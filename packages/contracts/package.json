{
  "name": "@workspace/contracts",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run --pool=threads",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:coverage": "vitest run --coverage --typecheck.enabled=false --exclude src/generated.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^15.1.0",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "@repo/vitest-config": "workspace:*",
    "@vitest/coverage-v8": "^4.1.9",
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  }
}
